ll sumr(ll l , ll r) //return the sum from range l to r
{
    return ((l + r) * (r - l + 1)) / 2ll;
}

int ceilLog(int n) {
    return std::__lg(2 * n - 1);
}

auto ok=[&](int i, ...){
  //code
  keep same data type in return
};
